{
  "name": "compodocs-workspaces-example",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "doc": "npx compodoc -c .compodocrc.json",
    "doc:serve": "yarn doc && npx compodoc -s"
  },
  "devDependencies": {
    "@compodoc/compodoc": "^1.1.10"
  }
}
